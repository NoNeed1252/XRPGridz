{
  "name": "xrpgridz",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p .output/static && cp index.html .output/static/ && tsc"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
